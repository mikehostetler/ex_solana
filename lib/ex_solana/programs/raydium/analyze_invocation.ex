defmodule ExSolana.Program.Raydium.PoolV4.AnalyzeInvocation do
  @moduledoc """
  Analyzes the invocation of the Raydium V4 Pool program.
  """
  # @impl ExSolana.ProgramBehaviour
  # def analyze_invocation(%Core.Invocation{instruction: :swap_base_out} = invocation) do
  #   %{max_amount_in: max_amount_in, amount_out: amount_out} = invocation.params
  #   [source_account, destination_account | _] = invocation.accounts
  # end
end
